plugins {
    id 'com.android.application'
}

android {
    namespace = 'com.epsilonchat.merkle'
    compileSdk = 36

    defaultConfig {
        applicationId = "com.epsilonchat.merkle"
        minSdk = 24
        targetSdk = 36
        versionCode = 2
        versionName = "0.5.0"
    }

    buildTypes {
        release {
            minifyEnabled = false
            // Use debug signing for easy distribution
            signingConfig = signingConfigs.debug
        }
        debug {
            minifyEnabled = false
        }
    }

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_17
        targetCompatibility = JavaVersion.VERSION_17
    }
}

dependencies {
}